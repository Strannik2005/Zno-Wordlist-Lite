<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.znolite">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/ZnoLite.MyApp">
        <activity
            android:name=".model.ResultQuiz"
            android:exported="false" />
        <activity
            android:name=".ActivityContentTests"
            android:exported="false" />
        <activity
            android:name=".TestsActivity"
            android:exported="false"
            android:label="@string/title_activity_tests"
            android:theme="@style/ZnoLite.MyApp.NoActionBar" />
        <activity
            android:name=".LogoActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true" />
    </application>

</manifest>